import React from "react";

const Form = () => {
  const [page, setPage] = useState(0);
  return (
    <div>
      <div>
        {/* header */}
        <div>{/* body */}</div>
        <div>
          <button>Prev</button>
          <button>Next</button>
        </div>
      </div>
    </div>
  );
};

export default Form;
